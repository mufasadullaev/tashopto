# Структура базы данных Opto

База данных SQLite хранится в: `%LOCALAPPDATA%\Opto\opto.db`

## Обзор

База данных состоит из **15 таблиц**, разделённых на три модуля:
- **Wyrabotka** (Выработка электроэнергии) — 4 таблицы
- **Baxta** (Ежедневный расчёт по вахтам) — 6 таблиц
- **Perejeg** (Пережоги топлива) — 3 таблицы
- **schema_version** — служебная таблица с версией схемы (текущая версия: 5)

Все данные **привязаны к конкретной дате** (формат: `YYYY-MM-DD`).

---

## 📊 Модуль Wyrabotka (Выработка электроэнергии)

### 1. `wyrabotka_day` - Основная запись дня
**Что хранит:** Факт наличия записи для конкретной даты

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK), формат: `2026-07-15` |
| `mode` | INTEGER | Режим: 0 = Расчёт, 1 = Перерасчёт |
| `saved_at` | TEXT | Когда сохранено (ISO timestamp) |

### 2. `wyrabotka_block` - Показания счётчиков по блокам
**Что хранит:** Показания генерации и собственных нужд для каждого из 12 энергоблоков

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `number` | INTEGER | Номер блока 1-12 (PK) |
| `generation_coefficient` | REAL | Коэффициент счётчика генерации (обычно 2880) |
| `generation_start` | REAL | Показания генерации на начало суток |
| `generation_end` | REAL | Показания генерации на конец суток |
| `own_needs_coefficient` | REAL | Коэффициент счётчика собственных нужд |
| `own_needs_start` | REAL | Показания СН на начало суток |
| `own_needs_end` | REAL | Показания СН на конец суток |
| `hours` | INTEGER | Количество часов работы блока |

**12 записей на дату** (по одной на каждый блок)

### 3. `wyrabotka_transformer` - Показания трансформаторов
**Что хранит:** Показания счётчиков трансформаторов собственных нужд

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `name` | TEXT | Название трансформатора (PK): ТС-20, ТС-30, 3ТР-А, 3ТР-В |
| `coefficient` | REAL | Коэффициент счётчика (обычно 2400) |
| `start_value` | REAL | Показания на начало суток |
| `end_value` | REAL | Показания на конец суток |

**4 записи на дату** (по одной на каждый трансформатор)

### 4. `wyrabotka_result` - Результаты расчёта
**Что хранит:** Итоговые рассчитанные значения за сутки и с начала месяца

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `day_hours` | INTEGER | Суммарное количество часов работы всех блоков |
| `day_generation` | REAL | Выработка за сутки (тыс. кВт·ч) |
| `day_own_needs` | REAL | Собственные нужды за сутки (тыс. кВт·ч) |
| `day_release` | REAL | Отпуск в сеть за сутки (тыс. кВт·ч) |
| `calculated_at` | TEXT | Когда рассчитано |

**1 запись на дату** (создаётся при нажатии "Расчёт")

---

## 🔥 Модуль Baxta (Расчёт по вахтам / смены)

### 5. `baxta_day` - Основная запись дня
**Что хранит:** Факт наличия записи для конкретной даты

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `mode` | INTEGER | Режим: 0 = Расчёт, 1 = Перерасчёт |
| `saved_at` | TEXT | Когда сохранено |

### 6. `baxta_meter` - Показания счётчиков по блокам
**Что хранит:** Показания генерации и СН в 4 временных точках (0:00, 8:00, 16:00, 24:00)

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `number` | INTEGER | Номер блока 1-12 (PK) |
| `generation_coefficient` | REAL | Коэффициент генерации |
| `generation_at0` | REAL | Показания генерации в 0:00 |
| `generation_at8` | REAL | Показания генерации в 8:00 |
| `generation_at16` | REAL | Показания генерации в 16:00 |
| `generation_at24` | REAL | Показания генерации в 24:00 |
| `own_needs_coefficient` | REAL | Коэффициент СН |
| `own_needs_at0` | REAL | Показания СН в 0:00 |
| `own_needs_at8` | REAL | Показания СН в 8:00 |
| `own_needs_at16` | REAL | Показания СН в 16:00 |
| `own_needs_at24` | REAL | Показания СН в 24:00 |

**12 записей на дату**

### 7. `baxta_coeff` - Коэффициенты для расчёта
**Что хранит:** Коэффициенты KL и KF для формул расчёта расхода топлива

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `number` | INTEGER | Номер блока 1-12 (PK) |
| `kl1` | REAL | Коэффициент KL1 (обычно 1.7) |
| `kl2` | REAL | Коэффициент KL2 (обычно 5.3) |
| `kf1` | REAL | Коэффициент KF1 |
| `kf2` | REAL | Коэффициент KF2 |
| `kf3` | REAL | Коэффициент KF3 |
| `kf4` | REAL | Коэффициент KF4 |

**12 записей на дату**

### 8. `baxta_thermo` - Теплотехнические параметры
**Что хранит:** Параметры работы котлов и турбин для каждого блока и каждой смены

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `block_number` | INTEGER | Номер блока 1-12 (PK) |
| `shift_index` | INTEGER | Номер смены 1-3 (PK) |
| `shift_label` | TEXT | Название смены: "0–8 ч", "8–16 ч", "16–24 ч" |
| `hours` | INTEGER | Количество часов работы в смене |
| `dro` | INTEGER | ДРО (дросселирование) |
| `pwd` | INTEGER | ПВД (подогреватель высокого давления) |
| `tpw` | REAL | Температура питательной воды (°C) |
| `tk` | REAL | Температура котла (°C) |
| `top` | REAL | Температура острого пара (°C) |
| `tpp` | REAL | Температура промежуточного пара (°C) |
| `pop` | REAL | Давление острого пара (атм) |
| `tug` | REAL | Температура уходящих газов (°C) |
| `thw` | REAL | Температура холодной воды (°C) |
| `o2` | REAL | Содержание кислорода в дымовых газах (%) |
| `tn` | INTEGER | Технологический номер |

**36 записей на дату** (12 блоков × 3 смены)

### 9. `baxta_plant` - Параметры электростанции
**Что хранит:** Общие параметры электростанции и режимные добавки

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `urp` | REAL | УРП (условное расчётное потребление) |
| `urm` | REAL | УРМ (условный расчётный минимум) |
| `tcb1` | REAL | Температура циркуляционной воды для 1-й смены |
| `tcb2` | REAL | Температура циркуляционной воды для 2-й смены |
| `tcb3` | REAL | Температура циркуляционной воды для 3-й смены |
| `wrmn11`-`wrmn34` | INTEGER | Режимные добавки для блоков 8-12 по сменам |
| `setn1k`-`setn5n` | INTEGER | Показания сетевых счётчиков (начало/конец) |
| `prises_json` | TEXT | Массив присосов (12 значений) в формате JSON |

**1 запись на дату**

### 10. `baxta_result` - Результаты расчёта
**Что хранит:** Итоговые рассчитанные значения по топливу

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `calculated_at` | TEXT | Когда рассчитано |
| `urp` | REAL | Расчётный УРП |
| `urm` | REAL | Расчётный УРМ |

**1 запись на дату**

### 11. `baxta_watch_schedule` — График вахт
**Что хранит:** Циклический шаблон назначения вахт на смены (karat `BAXTA0`)

| Поле | Тип | Описание |
|------|-----|----------|
| `row_index` | INTEGER | Номер строки 1–8 (PK) |
| `shift1_watch` | INTEGER | Вахта на 1-ю смену (1=А … 4=Г) |
| `shift2_watch` | INTEGER | Вахта на 2-ю смену |
| `shift3_watch` | INTEGER | Вахта на 3-ю смену |
| `resting_watch` | INTEGER | Отдыхающая вахта |
| `is_next` | INTEGER | 1 = строка для следующего первичного расчёта |
| `last_date_mmdd` | INTEGER | Дата последнего расчёта (ММДД) |

**8 записей** — шаблон из karat, цикл ~8 суток.

---

## ⛽ Модуль Perejeg (Пережоги топлива)

### 12. `perejeg_day` — Основная запись дня
**Что хранит:** Факт наличия записи для конкретной даты

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK), формат: `2026-07-15` |
| `mode` | INTEGER | Режим: 0 = Расчёт, 1 = Перерасчёт |
| `saved_at` | TEXT | Когда сохранено (ISO timestamp) |

### 13. `perejeg_row` — Исходные данные по строкам
**Что хранит:** Вводимые показатели по 16 строкам (N, T, DT, Эj) для 12 блоков и станции

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `row_index` | INTEGER | Номер строки 1–16 (PK) |
| `block1`–`block12` | REAL | Значение по блокам (для строк N/T/DT) |
| `station` | REAL | Значение по станции (Nсетев, Тсетев, Эj) |

**16 записей на дату** — по одной на каждую строку ввода

Строки ввода: Nрвв, N цн, Nпвд, Nснпнд, N оэ, Nсетев, N авар, Трвв, Т цн, Тпвд, Тснпнд, Т оэ, Тсетев, Т авар, DTавар, Эj.

### 14. `perejeg_result` — Результаты расчёта
**Что хранит:** Итоговые рассчитанные пережоги за сутки

| Поле | Тип | Описание |
|------|-----|----------|
| `date` | TEXT | Дата (PK) |
| `calculated_at` | TEXT | Когда рассчитано |
| `release_mln_kwh` | REAL | Эj — отпуск электроэнергии (млн кВт·ч) |
| `total_gkwh` | REAL | Итого пережогов (г/кВт·ч) |
| `result_json` | TEXT | Полный отчёт: 9 строк отклонений × 13 столбцов (12 блоков + станция) в JSON |

**1 запись на дату** (создаётся при нажатии «Расчёт»)

---

## 🔗 Связи между таблицами

### Модуль Wyrabotka
```
wyrabotka_day (главная)
    ├── wyrabotka_block (1 ко многим: 12 блоков)
    ├── wyrabotka_transformer (1 ко многим: 4 трансформатора)
    └── wyrabotka_result (1 к 1: итоги)
```

### Модуль Baxta
```
baxta_day (главная)
    ├── baxta_meter (1 ко многим: 12 блоков)
    ├── baxta_coeff (1 ко многим: 12 блоков)
    ├── baxta_thermo (1 ко многим: 36 записей = 12 блоков × 3 смены)
    ├── baxta_plant (1 к 1: параметры станции)
    └── baxta_result (1 к 1: итоги)
```

### Модуль Perejeg
```
perejeg_day (главная)
    ├── perejeg_row (1 ко многим: 16 строк ввода)
    └── perejeg_result (1 к 1: итоги)
```

**ВАЖНО:** При удалении записи из `wyrabotka_day`, `baxta_day` или `perejeg_day` автоматически удаляются все связанные записи благодаря `ON DELETE CASCADE`.

---

## 📝 Примеры количества записей

Для **одной даты**:

### Wyrabotka:
- 1 запись в `wyrabotka_day`
- 12 записей в `wyrabotka_block`
- 4 записи в `wyrabotka_transformer`
- 1 запись в `wyrabotka_result` (если был расчёт)
- **Всего: ~18 записей**

### Baxta:
- 1 запись в `baxta_day`
- 12 записей в `baxta_meter`
- 12 записей в `baxta_coeff`
- 36 записей в `baxta_thermo`
- 1 запись в `baxta_plant`
- 1 запись в `baxta_result` (если был расчёт)
- **Всего: ~63 записи**

### Perejeg:
- 1 запись в `perejeg_day`
- 16 записей в `perejeg_row`
- 1 запись в `perejeg_result` (если был расчёт)
- **Всего: ~18 записей**

---

## 🔍 Формат данных

- **Даты:** `YYYY-MM-DD` (например: `2026-07-15`)
- **Временные метки:** ISO 8601 (например: `2026-07-15T12:40:09.3317752+05:00`)
- **Числа:** Decimal (REAL в SQLite)
- **Текст:** UTF-8

---

## 💡 Особенности

1. **Независимость дат:** Данные каждой даты полностью независимы
2. **Нет автокопирования:** Данные с предыдущего дня не копируются автоматически
3. **Каскадное удаление:** Удаление дня удаляет все связанные записи
4. **Версионность:** `schema_version` хранит версию схемы БД для миграций
5. **Целостность:** `FOREIGN KEY` обеспечивает целостность данных
