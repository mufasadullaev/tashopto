# Отчет "Ежедневный расчет по вахтам" - Подробное описание расчетов

## 📊 СТРУКТУРА ОТЧЕТА

Отчет состоит из **4 секций**:
1. **1-СМЕНА** (вахта А)
2. **2-СМЕНА** (вахта Б)
3. **3-СМЕНА** (вахта В)
4. **ЗА СУТКИ** (итоговые суммы)

Каждая секция содержит **11 строк** × **12 блоков** + итоговую колонку.

---

## 📋 СТРОКИ ОТЧЕТА

| № | Строка | Код | Что показывает | Единицы |
|---|--------|-----|----------------|---------|
| 0 | **ЛБ** | LoadBase | Технологический номер блока (из ввода) | - |
| 1 | **НАГРУЗКА** | Load | Средняя нагрузка блока за смену | МВт |
| 2 | **ПУГ** | Pug | Пережоги от неполноты сгорания | кг |
| 3 | **ВАК** | Wak | Потери от охлаждения котла | кг |
| 4 | **ДОП** | Dop | Потери от вакуума | кг |
| 5 | **ТОП** | Top | Потери от температуры острого пара | кг |
| 6 | **ТПП** | Tpp | Потери от температуры промпара | кг |
| 7 | **СН** | Sn | Потери от собственных нужд | кг |
| 8 | **ТПВ** | Tpw | Потери от температуры питательной воды | кг |
| 9 | **ВСЕГО** | Total | Суммарный расход топлива | кг |
| 10 | **г/кВт·ч** | Gkwh | Удельный расход топлива | г/кВт·ч |

---

## 🔢 КАК СЧИТАЕТСЯ КАЖДАЯ СТРОКА

### 0️⃣ **ЛБ** (LoadBase) - Технологический номер

```
ЛБ = Tn (из поля "ТН" в теплотехнике)
```

**Источник:** Берётся напрямую из ввода (поле `Tn` в строке теплотехники).

---

### 1️⃣ **НАГРУЗКА** (Load) - Средняя нагрузка за смену

```
НАГРУЗКА = Выработка за смену / Часы работы
```

**Расчёт:**
```csharp
// Выработка за смену (МВт·ч)
wyr[block, shift] = КФЦ × 0.001 × (Показание_конец - Показание_начало)

// Средняя нагрузка (МВт)
load = wyr[block, shift] / Hours
```

**Где:**
- `КФЦ` - коэффициент выработки (константа для блока, например 2880)
- `Показание_конец` - показание счётчика на конец смены
- `Показание_начало` - показание счётчика на начало смены
- `Hours` - часы работы смены (обычно 8)

**Пример:**
```
Блок 1, 1-смена:
Показания: 0:00 = 460099.2, 8:00 = 460126.2
Дельта = 460126.2 - 460099.2 = 27.0
wyr = 2880 × 0.001 × 27.0 = 77.76 МВт·ч
load = 77.76 / 8 = 9.72 МВт
```

**В отчете (колонка "ВСЕГО"):**
- Для смен 1-3: **Среднее арифметическое** работающих блоков
- Для "ЗА СУТКИ": Среднее за все смены

---

### 2️⃣ **ПУГ** (Pug) - Пережоги от неполноты сгорания

```
EKSUT1 = 3.5 × (RRF - RRN) × Выработка
```

**Формула:**

```csharp
// 1. Расчёт нормального теплового баланса (RRN)
RRN = 5.15 + 0.0083 × na + 0.022 × (15.0 - ТХВ)

// 2. Расчёт фактического теплового баланса (RRF)
RRF = 0
if (na ≠ 0)
    RRF = Присоска × 0.01 × √(160.0 / na)

RRF += (21.0 - 0.1 × O2) / (21.0 - O2)
RRF = (RRF × 3.5 + 0.6) × (ТУГ - ТХВ) × 0.01

// 3. Итоговый перерасход
EKSUT1 = 3.5 × (RRF - RRN) × Выработка
```

**Где:**
- `na` = нагрузка (МВт)
- `ТХВ` = температура холодной воды (°C)
- `ТУГ` = температура уходящих газов (°C)
- `O2` = кислород (%)
- `Присоска` = значение присоски для данного блока
- `Выработка` = wyr (МВт·ч)

**Физический смысл:** Потери топлива из-за неполного сгорания (недожог, химический недожог).

---

### 3️⃣ **ВАК** (Wak) - Потери от охлаждения котла

```
EKSUT2 = 0.68 × Выработка × (ТК - RR)
```

**Формула:**

```csharp
RR = TCB + 0.07 × na + (20 + na) / (TCB + 17)
EKSUT2 = 0.68 × wyr × (ТК - RR)
```

**Где:**
- `TCB` = температура циркулирующей воды для данной смены (°C)
- `ТК` = температура котла (°C)
- `na` = нагрузка (МВт)
- `wyr` = выработка (МВт·ч)

**Физический смысл:** Потери тепла через стенки котла при его охлаждении.

---

### 4️⃣ **ДОП** (Dop) - Потери от вакуума

```
EKSUT3 = f(РОП, na) × Выработка
```

**Формула:**

```csharp
EKSUT3 = 0
if (РОП < 126.0)
{
    if (80.0 ≤ na ≤ 117.0)
        EKSUT3 = 0.148 × (na - 80.0) × wyr
    else if (117.0 < na ≤ 160.0)
        EKSUT3 = 0.127 × (160.0 - na) × wyr
}
```

**Где:**
- `РОП` = давление острого пара (кг/см²)
- `na` = нагрузка (МВт)
- `wyr` = выработка (МВт·ч)

**Физический смысл:** Дополнительные потери при работе на пониженном давлении (вакууме).

---

### 5️⃣ **ТОП** (Top) - Потери от температуры острого пара

```
EKSUT4 = -0.07 × Выработка × (ТОП - 540)
```

**Формула:**

```csharp
EKSUT4 = -0.07 × wyr × (ТОП - 540)
```

**Где:**
- `ТОП` = температура острого пара (°C)
- `540` = нормативная температура (°C)
- `wyr` = выработка (МВт·ч)

**Физический смысл:** 
- Если ТОП < 540°C → **перерасход** (знак +)
- Если ТОП > 540°C → **экономия** (знак -)

---

### 6️⃣ **ТПП** (Tpp) - Потери от температуры промпара

```
EKSUT5 = -0.056 × Выработка × (ТПП - RR_TPP)
```

**Формула:**

```csharp
// 1. Расчёт нормативной температуры
RR_TPP = na switch
{
    >= 70 and < 115  => 530 + 0.429 × (na - 80),
    >= 115           => 540,
    _                => 530
}

// 2. Расчёт потерь
EKSUT5 = (ТПП < 540) ? -0.056 × wyr × (ТПП - RR_TPP) : 0
```

**Где:**
- `ТПП` = температура промежуточного пара (°C)
- `na` = нагрузка (МВт)
- `wyr` = выработка (МВт·ч)

**Физический смысл:** Потери от отклонения температуры промпара от номинала.

---

### 7️⃣ **СН** (Sn) - Потери от собственных нужд

```
EKSUT6 = (СН_факт - СН_норм) × 350
```

**Формула:**

```csharp
// 1. Расчёт фактических СН (из показаний счётчиков)
esn[block, shift] = КФЦ_СН × 0.001 × (Показание_конец - Показание_начало)

// 2. Расчёт нормативных СН
СН_норм = Kf1 × Hours + Kf2 × wyr + regimeAddend

// 3. Режимная добавка (для блоков 8-12)
regimeAddend = block switch
{
    8       => 0.5 × WRMN[N5, shift],
    9       => 0.5 × WRMN[N6, shift],
    10      => 0.5 × WRMN[N7, shift],
    11 or 12 => 0.5 × WRMN[N4, shift],
    _       => 0
}

// 4. Потери
EKSUT6 = (esn - СН_норм) × 350
```

**Где:**
- `КФЦ_СН` = коэффициент собственных нужд (константа для блока, например 7200)
- `Kf1`, `Kf2` = коэффициенты нормативных СН (из таблицы коэффициентов)
- `Hours` = часы работы смены
- `WRMN` = время работы мазутных насосов (в часах)
- `350` = эмпирический коэффициент перерасхода

**Физический смысл:** Дополнительный расход топлива из-за превышения собственных нужд над нормативом.

---

### 8️⃣ **ТПВ** (Tpw) - Потери от температуры питательной воды

```
EKSUT7 = f(ПВД, ТПВ, ДРО, na) × Выработка
```

**Формула:**

```csharp
if (ПВД == 0)
{
    // Режим без ПВД
    EKSUT7 = (2.98 + 0.0276 × (na - 80.0)) × wyr
}
else
{
    // Режим с ПВД
    RR_TPW = (na < 150)
        ? 170 + Kl1 × na
        : Kl2 + 3 + 0.2 × (na - 150)
    
    if (na >= 160)
        RR_TPW = Kl2 + 3 + 0.2 × (na - 150)
    
    EKSUT7 = -0.066 × (ТПВ - RR_TPW + 0.05 × ДРО) × wyr
}
```

**Где:**
- `ПВД` = признак работы подогревателя высокого давления (0/1)
- `ТПВ` = температура питательной воды (°C)
- `ДРО` = деаэратор (°C)
- `Kl1`, `Kl2` = коэффициенты линейной зависимости
- `na` = нагрузка (МВт)
- `wyr` = выработка (МВт·ч)

**Физический смысл:** Потери из-за недостаточного подогрева питательной воды перед котлом.

---

### 9️⃣ **ВСЕГО** (Total) - Суммарный расход топлива

```
ВСЕГО = ПУГ + ВАК + ДОП + ТОП + ТПП + СН + ТПВ
```

**Формула:**

```csharp
ВСЕГО = EKSUT1 + EKSUT2 + EKSUT3 + EKSUT4 + EKSUT5 + EKSUT6 + EKSUT7
```

**Физический смысл:** Полный расход топлива (мазута) для данного блока за смену, в килограммах.

**В отчете (колонка "ВСЕГО"):**
- Для смен 1-3: **Сумма** по всем блокам
- Для "ЗА СУТКИ": Сумма за 3 смены

---

### 🔟 **г/кВт·ч** (Gkwh) - Удельный расход топлива

```
г/кВт·ч = ВСЕГО / Выработка
```

**Формула:**

```csharp
// Для каждого блока в каждой смене
gkbt[block, shift] = (wyr ≠ 0) ? Round2(ВСЕГО / wyr) : 0

// Среднее по станции для смены
gkbt[12, shift] = Среднее(gkbt[0..11, shift])  // только работающих блоков

// Среднее за сутки для блока
gkbt[block, 3] = Среднее(gkbt[block, 0..2])  // только работающих смен

// Среднее по станции за сутки
gkbt[12, 3] = Среднее всех суточных gkbt[0..11, 3]
```

**Физический смысл:** Удельный расход топлива - сколько грамм мазута тратится на выработку 1 кВт·ч электроэнергии.

**В отчете (колонка "ВСЕГО"):**
- Для смен 1-3: **Среднее** по работающим блокам
- Для "ЗА СУТКИ": Среднее за все смены

---

## 📐 АЛГОРИТМ ПОСТРОЕНИЯ ОТЧЕТА

### Шаг 1: Расчёт выработки и СН

Для каждого блока (1-12) и каждой смены (1-3):

```csharp
// Выработка (МВт·ч)
wyr[block, shift] = КФЦ × 0.001 × Δ(Показания_выработки)

// Собственные нужды (МВт·ч)
esn[block, shift] = КФЦ_СН × 0.001 × Δ(Показания_СН)
```

### Шаг 2: Расчёт 7 видов потерь (EKSUT1-7)

Для каждой строки теплотехники (36 строк = 12 блоков × 3 смены):

```csharp
load = wyr[block, shift] / Hours
ComputeFuel(...)  // Вычисляет EKSUT1..EKSUT7
```

Результаты сохраняются в массив `cif[block, row, shift]`:
- `cif[block, 0, shift]` = ЛБ (технологический номер)
- `cif[block, 1, shift]` = НАГРУЗКА
- `cif[block, 2, shift]` = ПУГ
- `cif[block, 3, shift]` = ВАК
- `cif[block, 4, shift]` = ДОП
- `cif[block, 5, shift]` = ТОП
- `cif[block, 6, shift]` = ТПП
- `cif[block, 7, shift]` = СН
- `cif[block, 8, shift]` = ТПВ

### Шаг 3: Расчёт суточных итогов

```csharp
// Суммирование по сменам (для строк 2-8)
for row = 2 to 8:
    cif[block, row, 3] = cif[block, row, 0] + cif[block, row, 1] + cif[block, row, 2]
```

### Шаг 4: Расчёт строки "ВСЕГО"

```csharp
// Для каждого блока и смены (включая сутки)
for block, shift:
    cif[block, 9, shift] = Σ(cif[block, 2..8, shift])
```

### Шаг 5: Расчёт удельного расхода (г/кВт·ч)

```csharp
// Для каждого блока и смены
gkbt[block, shift] = cif[block, 9, shift] / wyr[block, shift]

// Средние значения
gkbt[12, shift] = Среднее(gkbt[0..11, shift])  // по станции для смены
gkbt[block, 3] = Среднее(gkbt[block, 0..2])    // за сутки для блока
gkbt[12, 3] = Среднее(gkbt[0..11, 3])          // по станции за сутки
```

### Шаг 6: Формирование отчёта

Создаются 4 секции (3 смены + сутки), каждая с 11 строками и 13 колонками (12 блоков + итого).

---

## 🎯 ИТОГОВЫЕ ПОКАЗАТЕЛИ

В конце отчёта выводятся два ключевых показателя:

### УРП (Удельный расход топлива - план)

```
УРП = [значение из параметров станции]
```

**Источник:** Берётся из поля `Urp` в разделе "Параметры станции".

**Физический смысл:** Плановый удельный расход топлива на месяц (г/кВт·ч).

### УРМ (Удельный расход топлива - месяц)

```
УРМ = [значение из параметров станции]
```

**Источник:** Берётся из поля `Urm` в разделе "Параметры станции".

**Физический смысл:** Месячный удельный расход топлива (г/кВт·ч).

---

## 📊 ПРИМЕР ЧТЕНИЯ ОТЧЕТА

```
1-СМЕНА (вахта А)
┌────────┬────────┬────────┬───┬────────┬────────┐
│ Строка │ Бл. 1  │ Бл. 2  │...│ Бл. 12 │ ВСЕГО  │
├────────┼────────┼────────┼───┼────────┼────────┤
│ ЛБ     │  208   │  1539  │...│  5034  │        │
│ НАГР.  │  9.7   │  0.0   │...│  63.4  │  12.5  │ ← Средняя нагрузка по станции
│ ПУГ    │  143   │   0    │...│  931   │  1574  │ ← Суммарные пережоги
│ ВАК    │   27   │   0    │...│  175   │   296  │ ← Суммарные потери от охлаждения
│ ДОП    │    0   │   0    │...│    0   │     0  │ ← Нет потерь от вакуума
│ ТОП    │  -12   │   0    │...│  -79   │  -133  │ ← Экономия (ТОП > 540°C)
│ ТПП    │   -5   │   0    │...│  -33   │   -56  │ ← Экономия
│ СН     │   89   │   0    │...│  586   │   991  │ ← Перерасход СН
│ ТПВ    │  -38   │   0    │...│ -251   │  -424  │ ← Экономия (хороший подогрев)
│ ВСЕГО  │  204   │   0    │...│ 1329   │  2248  │ ← Общий расход топлива (кг)
│ г/кВт·ч│ 324.90 │  0.00  │...│ 324.90 │ 324.90 │ ← Удельный расход
└────────┴────────┴────────┴───┴────────┴────────┘
```

**Интерпретация:**
- Блок 1 выработал 77.6 МВт·ч при средней нагрузке 9.7 МВт
- Израсходовано 204 кг мазута
- Удельный расход = 324.90 г/кВт·ч (соответствует плану УРП)
- Основные потери: ПУГ (143 кг) и СН (89 кг)
- Есть экономия: ТОП (-12 кг), ТПП (-5 кг), ТПВ (-38 кг)

---

## ✅ ПРОВЕРКА КОРРЕКТНОСТИ

Для каждого блока должно выполняться:

```
ВСЕГО = ПУГ + ВАК + ДОП + ТОП + ТПП + СН + ТПВ
г/кВт·ч = ВСЕГО / Выработка
```

Для станции за смену:

```
ВСЕГО_станции = Σ ВСЕГО_блоков
Средний_удельный_расход = Среднее(г/кВт·ч работающих блоков)
```
